
  - platform: template
    lights:
      alexa_virtual:
        friendly_name: "Alexa Dummy Light"
        turn_on:

        turn_off:

        set_level:
      tv_volume:
        friendly_name: "TV Volume"
        value_template: >
            {% if is_state_attr("media_player.lg_tv_remote","is_volume_muted") %}
              'on'
            {% else %}
              'off'
            {% endif %}
        turn_on:

        turn_off:

        set_level:
        
  - platform: group
    name: Sitting Room
    entities:
      - light.lr1
      - light.lr2
      - light.lr3
  - platform: group
    name: Dining Room
    entities:
      - light.dr1
      - light.dr2
      - light.dr3
  - platform: group
    name: Living Room
    entities:
      - light.lr1
      - light.lr2
      - light.lr3
      - light.dr1
      - light.dr2
      - light.dr3
      
  
  - platform: switch
    name: Front room
    entity_id: switch.front_room
  - platform: switch
    name: Hall toilet
    entity_id: switch.hall_toilet
  - platform: switch
    name: Kitchen
    entity_id: switch.kitchen
  - platform: switch
    name: Master bedroom
    entity_id: switch.bedroom
  - platform: switch
    name: Outside
    entity_id: switch.outside
  - platform: switch
    name: Study 
    entity_id: switch.study
  - platform: switch
    name: Utility
    entity_id: switch.utility
  - platform: switch
    name: Francis 
    entity_id: switch.francis
  - platform: switch
    name: Maddy 
    entity_id: switch.maddy
  - platform: switch
    name: Hall
    entity_id: switch.hall_light
  - platform: switch
    name: Landing
    entity_id: switch.landing_light
  - platform: switch
    name: Bathroom Light
    entity_id: switch.bathroom_light
  - platform: switch
    name: En-Suite Light
    entity_id: switch.ensuite_light
    
    
    
    
  
  
    
        
  
  